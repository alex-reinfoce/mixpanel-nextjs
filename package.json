{
  "name": "mixpanel-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter playground dev",
    "build": "pnpm --filter mixpanel-nextjs build"
  },
  "workspaces": [
    "packages/*"
  ]
}
